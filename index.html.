<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <title>Contactar por WhatsApp</title>

  <!-- Meta Pixel -->
  <script>
    !function(f,b,e,v,n,t,s){
      if(f.fbq)return;n=f.fbq=function(){
        n.callMethod ? n.callMethod.apply(n,arguments) : n.queue.push(arguments)
      };
      if(!f._fbq)f._fbq=n;
      n.push=n;
      n.loaded=!0;
      n.version='2.0';
      n.queue=[];
      t=b.createElement(e);
      t.async=!0;
      t.src=v;
      s=b.getElementsByTagName(e)[0];
      s.parentNode.insertBefore(t,s)
    }(window, document,'script','https://connect.facebook.net/en_US/fbevents.js');

    fbq('init', '1515966293431253');  // Tu Pixel real
    fbq('track', 'PageView');
  </script>
</head>

<body style="font-family: sans-serif; text-align: center; margin-top: 50px;">
  <h2>Contactar por WhatsApp</h2>
  <p>Haz clic en el botón para abrir WhatsApp:</p>

  <!-- Botón compatible con Android, iOS y escritorio -->
  <a href="https://api.whatsapp.com/send?phone=527222044663" 
     target="_blank" 
     style="display: inline-block; padding: 15px 25px; font-size: 18px; background-color: #25D366; color: white; border-radius: 8px; text-decoration: none; cursor: pointer;"
     onclick="fbq('track', 'Contact');">
     Abrir WhatsApp
  </a>
</body>
</html>
